---
title: '{Project} documentation'
description: '{One line — what this project is and what these docs cover.}'
---

# {Project}

{One short paragraph: what the project does and who these docs are for.}

## Sections

- [{Section title}]({1.section-slug}/) — {one line}.

<!-- One bullet per section that was actually created, in tree order, plain text and no
     emoji. Link the real directory name, numeric prefix included, so the link resolves.
     Do not list a section that was dropped for having no page of its own — and if only
     one survived, one bullet is the correct answer. -->

{Optional: one line naming where the material that did NOT get a section lives instead —
a README, a schema, per-module docs — so a reader who expected it is not left guessing.}
