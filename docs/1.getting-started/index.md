---
title: 'Getting started'
description: 'Install the dev tooling, link the skills into Claude Code, and run the repository checks.'
---

# Getting started

Everything needed to work on this repository: install with pnpm, link the skills so Claude Code picks them up locally, and run the lint/format checks that CI mirrors.

The short version lives in the [root README](../../README.md); pages here cover the steps that need more than a command.
