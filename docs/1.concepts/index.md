---
title: 'Concepts'
description: 'How the skills fit together — the shared per-repo config, the AI workflow loops, and the release flow.'
---

Explanation rather than instruction: the models the skills share and why they are shaped that way.

A single skill's own reasoning stays with the skill (`DESIGN.md`). Pages here cover only what no single skill can own, because it is true of several at once.

::page-cards
::
