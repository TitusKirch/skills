# Fixture registry

A miniature of this repo's own shape, for `test/gen-skills.test.ts`. It is deliberately
**out of sync**: the table below is stale, so a `--check` run has something to find and a
`--write` run has something to fix.

<!-- skills:start -->
<!-- Generated by `pnpm skills:sync` — edit skill frontmatter, not this table. -->

### Repository & release

Commits, pull requests, releases and dependency updates — each driven by the repo's own conventions.

| Skill                                 | Description     |
| :------------------------------------ | :-------------- |
| [`alpha`](skills/repo/alpha/SKILL.md) | a stale summary |

<!-- skills:end -->
