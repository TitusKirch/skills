Fixture stand-in for the check-command contract. `isolated` is the base body plus the
install section, composed by the generator rather than written out twice.

<!-- verify:base -->

### Running the check

Fixture base verify body.

<!-- verify:isolated -->

Fixture install section.
